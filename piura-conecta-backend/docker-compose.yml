version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: piura_conecta_db
    restart: always
    environment:
      POSTGRES_USER: Sandro
      POSTGRES_PASSWORD: 9.bqEAA_kpp5p_i
      POSTGRES_DB: piuraconecta
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: